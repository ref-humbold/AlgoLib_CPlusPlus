os: linux
dist: bionic
addons:
  apt:
    packages:
      - libgtest-dev

language: cpp
compiler:
  - gcc
  - clang

before_install:
  - ./ci/install_gtest.sh
  - mkdir build
install:
  - cd ./build
  - cmake ..
  - make
script: cd ./build && ctest -T test
